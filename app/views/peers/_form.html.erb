<%= simple_form_for([@peer_group,@peer]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :contact_point %>
    <%= f.association :peer_group %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
